version: '3'

services:

  4.8-java8-ubuntu18:
    build:
      context: ./ubuntu-18
      dockerfile: ../Dockerfile
      args:
        JAVA_VERSION: 8-ubuntu18
        GRADLE_VERSION: '4.8'
    image: odaceo/gradle:4.8-java8-ubuntu18
